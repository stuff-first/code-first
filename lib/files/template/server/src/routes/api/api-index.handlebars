const router = require("express").Router();
{{#each models }}
const {{this.lower}}Routes = require("./{{this.lowerPlural}}");
{{/each }}

{{#each models }}
// {{this.upper}} routes
router.use("/{{this.lowerPlural}}", {{this.lower}}Routes);
{{/each }}

module.exports = router;
