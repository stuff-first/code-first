const router = require("express").Router();
const {{lowerPlural}}Controller = require("../../controllers/{{lowerPlural}}Controller");

{{#if associatiated }}
// Matches with "/api/{{lowerPlural}}"
router.route("/")
  .get({{lowerPlural}}Controller.findAll)
  .post({{lowerPlural}}Controller.create);

// Matches with "/api/{{lowerPlural}}/:id"
router
  .route("/:id")
  .get({{lowerPlural}}Controller.findById)
  .put({{lowerPlural}}Controller.update)
  .delete({{lowerPlural}}Controller.remove);
{{else}}
// Matches with "/api/{{lowerPlural}}/short"
router.route("/short")
  .get({{lowerPlural}}Controller.findAll);

// Matches with "/api/{{lowerPlural}}/short/:id"
router.route("/short/:id")
  .get({{lowerPlural}}Controller.findById);

// Matches with "/api/{{lowerPlural}}/full"
router.route("/full")
  .get({{lowerPlural}}Controller.findAllFull);

// Matches with "/api/{{lowerPlural}}/full/:id"
router.route("/full/:id")
  .get({{lowerPlural}}Controller.findByIdFull);

// Matches with "/api/{{lowerPlural}}"
router.route("/")
  .post({{lowerPlural}}Controller.create);

// Matches with "/api/{{lowerPlural}}/:id"
router
  .route("/:id")
  .put({{lowerPlural}}Controller.update)
  .delete({{lowerPlural}}Controller.remove);
{{/if}}

module.exports = router;